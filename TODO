API
===

* refactor Dataset to be more logical and to have official data setters/getters

Models
======

* default to parameterizing Gauss and Lorentz by area/integrated intensity
* convolution model

GUI
===

* arbitrary math. operations on datasets (x and y -> calc. dy automatically)
* combination of two datasets by math. operation
* add'l parameters
* export plot as python script / ipython notebook (?)
* sqwtas
* export parameters as ascii
* save mappings in file
* new session menu entry

Bugs
====

* str() -> unicode()
* fix initial guess Y axis chaos
